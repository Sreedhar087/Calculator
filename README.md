# Calculator
This Python calculator program performs basic operations like addition, subtraction, multiplication, and division. It displays a menu, accepts user choices, takes two numbers as input, calculates the result, handles division by zero, and continues running until the user selects the exit option. It provide a simple commandline calculator experience.
